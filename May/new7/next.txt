Now lets build the next part of this applet. 

Clicking the next button from step 3 will take the user to step 4. 

step 4: 
(empty the right panel)
Bring a graph in the left visual row. 
(below heading row)


(For drawing graph I have attached graph panel component in the components folder. You can take the style of the grid and the axis from there. But have the origin point in exact center of the grid, and to do that have a total of an even number of grid spaces. say -5 to +5 in y , and -8 to +8 in x. 
(do not render the labels of -5, 5, -8 and 8 as they might overlap with the arrows)

In the graph, bring everything in sequence, 
First plot A(3,5) , show coordinate label (right of the point)
Then plot B with coordinate label. 

Now draw the AB line, joining both the points. (Make sure this line has arrow heads on both its ends. This line will extend till the grid edge. So that this looks like this line is going to infinity in both directions )

Now make the y axis yellow, and thicken its stroke width a bit

Now draw and label A' and B' then join them with line similar to AB. 

Here the drawing ends. 

In right panel bring this content - 
"Line A’B’ 
is the image of 
line AB
after reflection across y-axis."


